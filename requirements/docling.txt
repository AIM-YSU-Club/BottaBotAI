# =============================================================================
# docling.txt — docling_worker 전용 의존성
# PyTorch는 Dockerfile에서 CUDA/CPU 휠을 따로 설치한다.
# =============================================================================

-r base.txt

docling>=2.0
