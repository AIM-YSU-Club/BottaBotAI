# =============================================================================
# reranker.txt — reranker_worker 전용 의존성
# =============================================================================
# 사용처: Dockerfile.reranker
# 베이스 이미지: pytorch/pytorch (CUDA) — PyTorch는 이미지에 포함, pip로 중복 설치 불필요
#
# 큐: reranker_queue
# 태스크: bottabot/celery/tasks/reranker.py
# =============================================================================

-r base.txt

# HuggingFace 기반 리랭킹 모델 (Cross-Encoder 등)
# TODO: 실제 사용할 모델/라이브러리에 맞게 버전·패키지 조정
#       (예: sentence-transformers, transformers, flashrank 등)
sentence-transformers>=3.0
